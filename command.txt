cat name.txt // выводит содержимое файла
echo [massage] >(>> не затирает старые данные) command.txt // вводит сообщение в данную команду
ls // ввыводит содержимое каталога
pwn // показывает в каком каталоге мы находимся
rm // удаляет файл
mrdir // создаёт каталог
rmdir // удаляет каталог
file name.// узнать информацию о файле
sudo pm-suspend // спящий режим pm-is-supported 
cd namekatalog // заходит в данный каталог
cp old new// копирует новый файл
find файл путь // ищёт файл
команда(ls) функция(-l) > name.txt // записывает данные в txt которые выводит данная команда
sh comand.txt // позваляет запустить команды из txt
ps // показывает действующие процессы
kill -9 номер процесса // позваляет закрыть процесс
exit // закрывает кансоль

glancess -чтобы посмотреть процессы

git init // нужно инициализировать в той папке, где хочешь, чтобы от туда считывались файлы.
git add namefile //
git status // проверяет статус работы git 
